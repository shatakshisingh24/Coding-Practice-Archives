#include <bits/stdc++.h>
#include<math.h>
using namespace std;

int main()
{
  //write your code here
  int test;
  cin>>test;
  
  for(int i=0;i<test; i++)
  {
    long long int x;
    cin>>x;
    
    cout<<x<<" ";
    
    while(x>1)
    {
      if(x%2==0)
      {
        x=floor(sqrt(x));
        cout<<x<<" ";
      }
      
      else if(x%2!=0)
      {
        x= floor(sqrt(x)*sqrt(x)*sqrt(x));
        cout<<x<<" ";
      }
    }
    cout<<endl;
  }
  
  
  return 0;
}
